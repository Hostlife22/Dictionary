export enum ProtectedRouteRoles {
  ALLOWED = 'ALLOWED',
  NOT_ALLOWED = 'NOT_ALLOWED',
}

export const enum SettingsNameEnum {
  SOUND = 'sound',
  TEXTBOOK = 'textbook',
  AUDIO = 'audioCall',
  SPRINT = 'sprint',
}

export const enum UserWordStatus {
  WORK = 'work',
  HARD = 'hard',
  DELETE = 'delete',
}

export const enum MethodsEnum {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  DELETE = 'DELETE',
}
